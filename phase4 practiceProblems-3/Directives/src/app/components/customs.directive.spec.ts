import { CustomsDirective } from './customs.directive';

describe('CustomsDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomsDirective();
    expect(directive).toBeTruthy();
  });
});
